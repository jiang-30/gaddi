import type { IFormFieldAttrs } from '../type'
import { tools } from '../../../utils'

export const formFieldCascaderFormat = (field: IFormFieldAttrs) => {
  const defaultFieldAttrs = tools.defaultFieldAttrs

  return {

  }
}


